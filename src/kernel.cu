#include "kernel.h"
#include <cuda_runtime.h>
#include <stdio.h>

__global__ void compute_acceleration(base_particle other) {
    // TODO: define kernel function
}

__global__ void compute_acceleration(Container particles) {
    // TODO: define kernel function
}